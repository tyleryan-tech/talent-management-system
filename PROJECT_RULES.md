# PROJECT_RULES.md

本文档给开发者和 AI 共同使用，用来快速理解人才管理系统的核心原则。更详细的背景见 `README.md`、`server/README.md` 和 `.cursor/rules/`。

## 1. 系统定位

本项目是人才管理原型系统，覆盖花名册、组织、绩效、人才盘点、考勤、招聘、培训和用户管理。当前线上主架构是 Vercel 静态前端 + Serverless Express API + SQLite 临时数据；适合演示，不适合作为生产持久数据库。

## 2. 权限与数据范围

- HRBP / 超级管理员可访问全量工作区，也可按组织范围查看部门子树。
- 汇报经理只能访问本人和递归下属相关数据。
- 产品线负责人具备接近超级管理员的管理权限。
- 角色权限必须在页面、store、API、写入校验中一致体现。

## 3. 数据一致性

- 员工信息、组织关系、绩效、考勤等跨模块共用数据必须保持一致。
- 同一员工的最新已生效版本是权威状态。
- 多角色修改同一员工时，未经审批的变更不得提前生效。
- 修改一个模块时，要检查关联模块是否需要同步调整。

## 4. 产品线与持久化

- 产品线数据按 `tm_L{lineId}_*` 存储，互相隔离。
- `users` 等全局数据不属于单个产品线，不得被作用域清理逻辑修改。
- 切换产品线、周期、角色等上下文时，必须先同步保存，再切换，再重新加载。
- 从 localStorage / IndexedDB 加载数据时，不假设数据干净，要做去重和完整性校验。

## 5. 导入与字段映射

所有上传导入功能默认第一行为字段名。系统应通过字段含义匹配，而不是列位置匹配。新增导入入口必须维护 schema 的 `key`、`label`、`aliases`、`keywords`，并使用统一字段映射能力让用户确认不确定映射。

## 6. 测试要求

项目使用浏览器端测试入口 `tests/index.html`。修改以下内容后应运行全量测试并补充必要用例：

- store action、computed、持久化逻辑
- 产品线切换和 hydrate 生命周期
- 权限范围和服务端裁剪
- 绩效审批、驳回、周期创建
- Excel / 文件导入
- 跨模块数据同步

## 7. 技术与部署原则

- 方案设计按约 1 万名员工规模评估。
- 用户可见提示和错误文案使用中文。
- 生产环境必须具备持久存储、访问控制、日志、监控和告警。
- Vercel `/tmp` 数据会丢失；生产应使用云数据库或独立 Node / Go 后端。
- WebSocket 在 Vercel Serverless 不可用，应保留轮询回退。

## 8. 后续优化优先级

1. 先保护数据一致性和权限边界。
2. 再优化用户流程和界面体验。
3. 再考虑性能、可观测性和部署演进。
4. 避免只修补单页表现而留下跨模块旧逻辑。
