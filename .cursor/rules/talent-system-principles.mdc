---
description: 人才管理系统——权限与数据一致性、新功能协作、改动重构与技术选型原则
alwaysApply: true
---

# 人才管理系统：权限、一致性、改动与技术选型原则

## 1. 用户权限与数据一致性

- **登录与角色**：用户使用邮箱登录；系统根据邮箱自动匹配角色（如 HRBP、汇报经理等），其可访问的组织与信息范围必须受该角色约束，在 UI 与 API/数据层一致体现。
- **多角色写同一员工**：多名不同角色若修改同一位员工的任意信息，**未经超级权限用户审批的变更不得生效**（不写入权威数据源、不向用户展示为已保存）。
- **单一事实来源**：同一员工的全量信息须保持一致；**以最近一次已生效（含审批通过）的更新为准**；读路径应反映该权威状态，避免各模块各存一份互不同步的数据。

**示例**：HRBP 修改职级已保存，汇报经理再改同一字段 → 若需审批，在审批通过前其他视图仍显示上一生效版本；通过后全模块以新值为准。

## 2. 新功能与模块协作

- 新增功能在 **前端展示、数据模型/同步、与其他模块的交互** 上须逻辑自洽；若与现有规则冲突，**可先对照业内常见做法（如审批流、RBAC、主数据单一来源）**，再落到本系统的实现方案中。
- 避免只改一个页面导致其他入口仍展示旧逻辑或旧数据。

## 3. 功能改动原则

- **视觉与交互**：改动后的界面须符合常见阅读习惯与操作习惯（信息层级、反馈、空态/错误态），不为了赶工牺牲可理解性。
- **影响面**：若改动波及其他模块，须 **同时审视当前功能与被影响功能**，必要时做 **重构或统一设计**，使新旧能力在同一套用户心智下可用，而不是补丁堆叠。

**示例**：调整「员工档案」字段若被「报表」「审批」引用，需一并更新展示规则、校验与数据流，避免一处新、一处旧。

## 4. 技术选型与运维、安全

- **规模**：所有技术选型须按 **约 1 万名员工** 的规模评估（并发、数据量、查询与批量任务、存储与索引、缓存与限流等），避免默认按小团队体量设计。
- **上线与可观测**：系统部署上线后应能稳定运行；须规划 **告警与监控**（关键指标、异常、可用性），在出现故障或异常时 **能自动上报/记录**；面向终端用户的 **报错与提示文案使用中文**，便于一线用户理解与反馈。
- **安全**：满足 **信息安全与网络安全** 的基本要求（如传输与存储加密、访问控制、最小权限、依赖与漏洞管理、日志与审计、合规的数据处理等），选型与实现不得明显违背常见基线。

**示例**：选数据库或消息队列时写明在万级员工下的容量与扩展路径；错误处理区分「用户可见中文提示」与「运维侧详细日志/告警」。

## 5. 文件上传与字段映射原则

所有涉及文档上传导入数据的功能，统一遵守以下规则：

- **自动字段识别**：默认上传文档第一行为字段名称。系统使用 `TM.fieldMapper`（`js/utils/fieldMapper.js`）对上传文件的列名与系统预定义字段进行模糊匹配（Dice 系数 + 子串包含 + 同义词库），自动推断映射关系。
- **确认对话框**：当匹配置信度不全为"精确"时，弹出 `FieldMapDialog` 组件让用户确认或调整映射关系；如所有匹配均为精确匹配（≥0.95），可直接导入不弹窗。
- **顺序无关**：上传文件的列顺序无需与系统显示顺序一致，系统按字段含义（而非位置）进行映射，导入后按系统原有顺序编排显示。
- **同义词库维护**：每个上传入口须定义自己的 schema（含 `key`、`label`、`aliases`、`keywords`），涵盖中英文常见写法。新增字段时必须同步更新 schema 的 aliases/keywords。
- **已应用模块**：
  - **Recruitment Pipeline**（`Recruitment.js` → `PIPE_SCHEMA` + `uploadPipeline`）
  - **Roster 花名册**（`Roster.js` → `ROSTER_SCHEMA` + `importExcel` / `appendImportExcel`）
  - **Attendance 考勤**（`Attendance.js` → 已有智能时间序列列名检测，遵循相同自动识别原则）
- **新增上传入口**：任何新模块若需文件导入，必须使用 `TM.fieldMapper.match(schema, headers)` + `FieldMapDialog` 组件，不得硬编码列位置。

## 6. 持久化与数据流安全（防止静默数据丢失/污染）

本系统的数据按产品线隔离存储（`tm_L{lineId}_{key}`），users 为全局存储（`tm_global_users`）。以下规则强制执行，违反即为 P0 级缺陷。

### 6.1 上下文切换前必须同步落盘

- 任何会改变"写入目标"的操作（切换产品线、切换周期、切换角色等），**必须在改变上下文标识之前同步写入所有脏数据**。
- **禁止**在切换路径中使用防抖（`setTimeout`/`debounce`）写入——防抖回调触发时上下文可能已经变了，数据会写入错误位置。
- 正确模式：`flushSync()` → 改 `contextId` → `hydrate()`。

### 6.2 全局数据与作用域数据严格分离

- **全局数据**（`users`、产品线注册表等跨产品线共享的数据）的关联字段（如 `user.employeeId`）**不得被作用域操作修改**。
  - 例：当前产品线没有某员工，不代表该用户的 `employeeId` 应被清空——其他产品线可能有该员工。
- **作用域数据**（`employees`、`departments` 等按产品线隔离的数据）的同步/清理逻辑不得触及全局数据的结构。
- 新增任何 `persist`/`save`/`flush` 调用时，必须确认 `currentLineId` 是否是预期的写入目标。

### 6.3 加载时防御性校验

- 从存储加载数组型数据时，**必须按主键去重**（`employees` 按 `id`、`users` 按 `id`/`email` 等）。检测到重复时写 `console.warn` 日志并自动修复存储。
- **不得假设存储中的数据是干净的**——存储可能被其他 tab、旧版本、或历史 bug 污染过。加载后应做基本完整性检查（主键唯一、必填字段非空、外键引用有效等）。

### 6.4 hydrate/init/switch 生命周期纪律

- `hydrate()` 开始时必须 **清空所有定时器和脏标记**（`_persistTimer`、`_dirtyKeys`），确保新数据加载不被旧上下文的残留状态污染。
- `hydrate()` 结束时的 `persistAllSync()` 只应写入 hydrate 过程中新产生的脏键，不应包含上一个上下文遗留的脏键。
- 修改任何生命周期方法时，必须画出完整的时序：`markDirty → persist → contextSwitch → hydrate → persist`，确认每一步的 `lineId` 和 `dirtyKeys` 状态。

### 6.5 变更自检清单

修改 store 的 action 或持久化逻辑时，必须逐项确认：

1. **写入目标**：这次写入的 scope/lineId 是哪个？有没有可能在异步间隙被其他操作改变？
2. **脏标记**：`_markDirty` 标记的键是否准确？有没有多标（导致无关数据被覆写）或少标（导致修改丢失）？
3. **全局 vs 作用域**：这次操作涉及的数据是全局的还是作用域的？有没有跨界修改？
4. **并发安全**：如果用户在写入完成前做了其他操作（切换页面、切换产品线、关闭浏览器），数据会丢失吗？
5. **幂等性**：如果这个操作被执行两次（页面刷新、重复点击），数据会重复吗？

## 7. 测试纪律（防止回归、确保交付质量）

本项目使用浏览器端测试运行器（`tests/index.html`），分三层覆盖：**Store 逻辑** → **Computed 视图** → **Smoke 端到端**。以下规则强制执行。

### 7.1 何时必须运行测试

- **每次修改 store action、computed 属性、或持久化逻辑后**，必须在 `tests/index.html` 中运行全部测试并确认通过。
- **提交代码前**，测试必须全部通过。

### 7.2 新功能必须同步添加测试

- 新增 store action → 至少一个"正向"用例 + 一个"边界/异常"用例。
- 新增 computed / 视图过滤逻辑 → 在 `test-view-computed.js` 中断言输出。
- 新增跨模块交互 → 在 `test-smoke.js` 中添加端到端场景。

### 7.3 修复 bug 必须添加回归测试

- 每个已修复的 bug 必须有对应的测试用例，命名/注释中注明对应的问题描述。
- 回归测试应放在最匹配的测试文件中（数据一致性 → `test-data-consistency.js`，视图层 → `test-view-computed.js`，等）。

### 7.4 高风险操作的必测清单

以下操作类型历史上反复出问题，修改时**必须**有对应测试覆盖：

| 操作类型 | 必测内容 |
|----------|---------|
| 产品线切换 | 切换前后数据隔离、脏键清理、hydrate 一致性 |
| 员工状态变更 | leave 排除、活跃计数、九宫格同步 |
| 绩效周期创建 | 参评人数 = 活跃员工、cutoffDate 过滤、review 字段完整 |
| 绩效审批/驳回 | 整批一致性、状态流转正确、权限校验 |
| 数据导入 | 去重、字段映射、关联数据同步 |

### 7.5 测试文件组织

| 文件 | 覆盖范围 |
|------|---------|
| `test-employee-crud.js` | 员工增删改 |
| `test-organization.js` | 部门/岗位管理 |
| `test-performance-workflow.js` | 绩效基础操作 |
| `test-data-sync.js` | 跨模块数据同步 |
| `test-data-consistency.js` | 数据一致性回归 |
| `test-product-line-switch.js` | 产品线切换隔离 |
| `test-team-reject.js` | 绩效整批驳回 |
| `test-view-computed.js` | 视图层 computed 断言 |
| `test-smoke.js` | 端到端业务流程 |
